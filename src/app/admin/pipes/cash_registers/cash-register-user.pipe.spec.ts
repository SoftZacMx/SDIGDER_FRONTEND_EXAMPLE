import { CashRegisterUserPipe } from './cash-register-user.pipe';

describe('CashRegisterUserPipe', () => {
  it('create an instance', () => {
    const pipe = new CashRegisterUserPipe();
    expect(pipe).toBeTruthy();
  });
});
