import { BussinesServicesPipe } from './bussines-services.pipe';

describe('BussinesServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new BussinesServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
