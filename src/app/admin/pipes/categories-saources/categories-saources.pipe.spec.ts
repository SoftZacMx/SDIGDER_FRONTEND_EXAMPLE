import { CategoriesSaourcesPipe } from './categories-saources.pipe';

describe('CategoriesSaourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriesSaourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
